---
title: Quickstart
description: Get from zero to your first working Codebuff example in 5 minutes
---

Get started with Codebuff in minutes. This guide will walk you through installation, setup, and your first coding task.

## Getting started

<Steps>
  <Step title="Install the CLI">
    Install Codebuff globally using npm:

    ```bash
    npm install -g codebuff
    ```

    <Note>
      Codebuff requires Node.js 18 or higher. Check your version with `node --version`.
    </Note>
  </Step>

  <Step title="Navigate to your project">
    Change to your project directory:

    ```bash
    cd your-project
    ```

    <Note>
      Codebuff works best with Git repositories, but it's not required.
    </Note>
  </Step>

  <Step title="Start Codebuff">
    Launch the interactive CLI:

    ```bash
    codebuff
    ```

    The first time you run Codebuff, you'll be prompted to authenticate with your Codebuff account.
  </Step>

  <Step title="Give your first instruction">
    Try asking Codebuff to make a change to your codebase:

    ```
    Add error handling to the user registration function
    ```

    Codebuff will:
    - Analyze your codebase structure
    - Find relevant files
    - Make precise edits
    - Validate the changes

    <Note>
      You can also use the `/init` command to create a `knowledge.md` file and `.agents/` directory for custom agent development.
    </Note>
  </Step>

  <Step title="Review and iterate">
    After Codebuff completes its task, review the changes:

    ```bash
    git diff
    ```

    Continue the conversation with follow-up instructions:

    ```
    Also add input validation for the email field
    ```

    Codebuff maintains context from previous exchanges in the session.
  </Step>
</Steps>

## First task examples

Here are some examples to try with Codebuff:

<CodeGroup>
```bash Fix bugs
codebuff
> Fix the SQL injection vulnerability in user registration
```

```bash Add features
codebuff
> Add rate limiting to all API endpoints
```

```bash Refactor code
codebuff
> Refactor the database connection code for better performance
```

```bash Add tests
codebuff
> Add unit tests for the authentication module
```
</CodeGroup>

## Using with the SDK

If you want to integrate Codebuff into your application, install the SDK instead:

```bash
npm install @codebuff/sdk
```

Then use it in your code:

```typescript
import { CodebuffClient } from '@codebuff/sdk'

const client = new CodebuffClient({
  apiKey: process.env.CODEBUFF_API_KEY,
  cwd: process.cwd(),
})

const result = await client.run({
  agent: 'base',
  prompt: 'Add error handling to all API endpoints',
  handleEvent: (event) => {
    console.log('Progress:', event)
  },
})
```

<Note>
  Get your API key from [codebuff.com/api-keys](https://www.codebuff.com/api-keys).
</Note>

## What's next?

<CardGroup cols={2}>
  <Card title="CLI Guide" icon="terminal" href="/cli/overview">
    Learn advanced CLI commands and workflows
  </Card>
  <Card title="SDK Guide" icon="code" href="/sdk/overview">
    Build Codebuff into your applications
  </Card>
  <Card title="Agent Development" icon="robot" href="/agents/creating-agents">
    Create custom agents with specialized workflows
  </Card>
  <Card title="Custom Tools" icon="wrench" href="/sdk/custom-tools">
    Extend Codebuff with your own tools
  </Card>
</CardGroup>

## Need help?

<CardGroup cols={2}>
  <Card title="Discord Community" icon="discord" href="https://codebuff.com/discord">
    Get help from the community
  </Card>
  <Card title="GitHub Issues" icon="github" href="https://github.com/CodebuffAI/codebuff/issues">
    Report bugs or request features
  </Card>
  <Card title="Email Support" icon="envelope" href="mailto:support@codebuff.com">
    Contact the team directly
  </Card>
  <Card title="Documentation" icon="book" href="https://codebuff.com/docs">
    Browse the full documentation
  </Card>
</CardGroup>